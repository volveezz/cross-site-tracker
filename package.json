{
	"name": "cross-site-tracker",
	"type": "module",
	"private": true,
	"scripts": {
		"dev:a": "bun run --watch src/serverA.ts",
		"dev:x": "bun run --watch src/serverX.ts",
		"start:a": "bun run src/serverA.ts",
		"start:x": "bun run src/serverX.ts"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"hono": "^4.11.1"
	}
}
