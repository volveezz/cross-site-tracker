{
	"name": "cross-site-tracker",
	"type": "module",
	"private": true,
	"scripts": {
		"dev:a": "bun run --watch src/devA.ts",
		"dev:x": "bun run --watch src/devX.ts",
		"start:a": "bun run src/devA.ts",
		"start:x": "bun run src/devX.ts"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@vercel/node": "^5.5.16"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"hono": "^4.11.1"
	}
}
